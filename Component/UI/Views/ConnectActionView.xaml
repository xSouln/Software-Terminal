<UserControl x:Class="Terminal.UI.ConnectActionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Terminal.UI"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="200" Style="{StaticResource UserControlGlobalStyle}" Background="Transparent" VerticalContentAlignment="Top">
    <Grid>
        <Button x:Name="ButConnectAction" Height="auto" Content="{Binding ConnectionStateName}" Background="{Binding ConnectionStateBackground}" IsEnabled="{Binding ConnectionIsAvailable}" Margin="0,0,0,0" Style="{StaticResource ButtonStyle1}" Width="110" Grid.Column="1" Click="ButConnectAction_Click"/>
    </Grid>
</UserControl>
